import { MainLayout } from '@components/mixins/MainLayout';
import styled from 'styled-components';

export const BackButtonWrapper = styled.div`
  margin: 19px 0;
`;

export const Layout = styled(MainLayout)`
  padding: 0 19px;
`;
